# تقرير صحة موقع وضوح (وضوح | Wuduh)

**التاريخ:** 27 يوليو 2026 · **الموقع:** https://wuduh.tasweeqat.com · **النوع:** فحص للقراءة فقط (بدون تسجيل دخول أو إرسال نماذج)

**حالة الموقع:** يعمل ✅ — استجابت الصفحة الرئيسية وأعادت محتوى HTML كاملاً بنجاح.

> ملاحظة عن نطاق الفحص: لم تكن إضافة المتصفح (Claude in Chrome) متصلة في هذه الجولة، لذا تعذّر التقاط لقطات الشاشة وفحص أخطاء الكونسول والتحقق البصري من اتجاه النص (RTL) ولافتة Clerk «Development mode». تم الفحص على مستوى المحتوى (HTML) لجميع الصفحات الست. يُنصح بإعادة التشغيل مع اتصال المتصفح لإكمال الفحص البصري.

---

## (أ) مشاكل حرجة

- **روابط الصور الاجتماعية تشير إلى عنوان IP خام عبر HTTP غير آمن.** في كل الصفحات، وسوم `og:image` و`twitter:image` و`og:url` تشير إلى `http://187.124.51.48` بدل النطاق. هذا يكسر معاينة المشاركة على واتساب/تويتر/لينكدإن، ويكشف عنوان IP الخادم الأصلي علناً، ويسبب «محتوى مختلط» (mixed content) على موقع يعمل بـ HTTPS.
- **لم يُتحقق من لافتة Clerk «Development mode» / مفاتيح الاختبار.** صفحتا «تسجيل الدخول» و«إنشاء حساب» تُبنيان بالكامل عبر JavaScript (Clerk) ولا تعرضان أي محتوى في فحص HTML (شعار فقط). يجب فحص هاتين الصفحتين بصرياً في المتصفح للتأكد من عدم استخدام مفاتيح تطوير قبل الإطلاق العام — يبقى هذا بنداً مفتوحاً حتى الفحص البصري.

## (ب) مشاكل بسيطة / تجميلية

- **عناوين صفحات غير مخصّصة.** صفحات «تواصل» و«تسجيل الدخول» و«إنشاء حساب» تستخدم عنوان الرئيسية العام نفسه بدل عنوان خاص بكل صفحة (صفحتا «التسعير» و«من نحن» صحيحتان). يضعف SEO وتجربة التبويبات.
- **تعارض في إصدار PMBOK.** النصوص عبر الموقع تذكر دعم الإصدارين 7 و 8، لكن تذييل كل الصفحات يذكر «PMBOK Guide 7th Edition» فقط. يُوحَّد.
- **صفحتا المصادقة فارغتان بدون JavaScript.** لا يوجد تصيير من الخادم (SSR) لصفحتي الدخول/التسجيل — تظهر بالشعار فقط عند التحميل الأولي، ما يضعف الأداء المُدرَك و SEO.

## (ج) اقتراحات تحسين مستقبلية

- توحيد كل روابط الميتا الاجتماعية (`og:image`, `og:url`, `twitter:image`) على النطاق `https://wuduh.tasweeqat.com` بدل عنوان الـ IP.
- إضافة عنوان ووصف ميتا مخصّصين لكل صفحة (تواصل، دخول، تسجيل).
- إضافة `sitemap.xml` و`robots.txt` وبيانات مُنظّمة (Product/Offer schema) لصفحة التسعير لتحسين الظهور في محركات البحث.
- تحسين أداء صفحات المصادقة (skeleton أو SSR) لتفادي ظهور الشعار وحده أثناء التحميل.
- إعادة تشغيل الفحص مع اتصال إضافة المتصفح لتغطية التحقق البصري من التخطيط واتجاه RTL وأخطاء الكونسول ولافتة Clerk.

---

*الصفحات المفحوصة: الرئيسية · التسعير · من نحن · تواصل · تسجيل الدخول · إنشاء حساب.*
